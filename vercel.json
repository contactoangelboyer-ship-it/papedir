{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/papedir run build",
  "outputDirectory": "artifacts/papedir/dist/public",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}